package com.example.ixltask.utils

object DataUtils {
    val expList = List(5) { it+1 }
    val accountTypes = List(5) { "Account Type ${it+1}" }
    val bankList = List(5) { "Bank ${it + 1}" }
    val branchList = List(5) { "Branch ${it + 1}" }

}